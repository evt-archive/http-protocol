Gem::Specification.new do |s|
  s.name        = "http-kit"
  s.version     = "0.1.0"
  s.licenses    = ["MIT"]
  s.summary     = "HTTP client library designed to expose the underlying socket"
  s.description = "HTTP client library designed to expose the underlying socket"
  s.authors     = ["Nathan Ladd"]
  s.email       = "nathanladd+github@gmail.com"
  s.files       = Dir["lib/**/*.rb"]
  s.homepage    = "https://github.com/ntl/http-kit"
  s.executables = []
end
